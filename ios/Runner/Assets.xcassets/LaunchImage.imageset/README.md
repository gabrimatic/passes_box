# Launch Screen Assets

Replace the image files in this directory to customize the launch screen.

You can also open the iOS workspace with `open ios/Runner.xcworkspace`, select `Runner/Assets.xcassets`, and drop the images into the Project Navigator.
